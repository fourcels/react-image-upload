{
  "scripts": {
    "shadcn:dev": "pnpm run dev & pnpm run -F shadcn dev",
    "docs:dev": "pnpm run dev & pnpm run -F docs start",
    "docs:build": "pnpm run build && pnpm run -F docs build",
    "docs:serve": "pnpm run -F docs serve",
    "dev": "pnpm run -F react-image-upload dev",
    "build": "pnpm run -F react-image-upload build",
    "release": "pnpm build && pnpm changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10"
  }
}
